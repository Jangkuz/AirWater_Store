meta {
  name: Bask HealthCheck
  type: http
  seq: 5
}

get {
  url: {{basket_url}}/health
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.status: eq Healthy
}

settings {
  encodeUrl: true
  timeout: 0
}
