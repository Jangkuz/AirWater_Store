meta {
  name: Basket Checkout
  type: http
  seq: 3
}

post {
  url: {{basket_url}}/basket/checkout
  body: json
  auth: inherit
}

body:json {
  {
      "UserId": {{userSeedId}}
  }   
}

assert {
  res.status: eq 200
  res.body.isSuccess: isTruthy
}

settings {
  encodeUrl: true
  timeout: 0
}
