meta {
  name: Basket Delete
  type: http
  seq: 4
}

delete {
  url: {{basket_url}}/basket/1
  body: none
  auth: inherit
}

body:json {
  {
      "UserId": {{userSeedId}}
  }   
}

assert {
  res.status: eq 200
  res.body.isSuccess: isTruthy
}

settings {
  encodeUrl: true
  timeout: 0
}
