meta {
  name: Game GetById
  type: http
  seq: 2
}

get {
  url: {{catalog_url}}/games/{{gameSeedId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.game: isDefined
}

settings {
  encodeUrl: true
  timeout: 0
}
