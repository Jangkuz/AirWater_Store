meta {
  name: Catalog HealthCheck
  type: http
  seq: 11
}

get {
  url: {{catalog_url}}/health
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.status: eq Healthy
}

settings {
  encodeUrl: true
  timeout: 0
}
